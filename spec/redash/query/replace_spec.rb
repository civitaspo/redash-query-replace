RSpec.describe Redash::Query::Replace do
  it "has a version number" do
    expect(Redash::Query::Replace::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
